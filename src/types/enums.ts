export enum SessionStatus {
  Idle = "idle",
  Loading = "loading",
  Authenticated = "authenticated",
  Error = "error",
}

export enum HistoryRecordState {
  Open = "open",
  Closed = "closed",
  Aberto = "aberto",
  Fechado = "fechado",
}
